export default class Product {
    constructor(id, title, price, inventoryCount) {
        this.id = id;
        this.title = title;
        this.price = price;
        this.inventory_count = inventoryCount;
    }
}
